services:
  users_service:
    # Build a custom image  for the users service
    build:
      context: .
      target: development
      dockerfile: Dockerfile
    env_file:
      - .env
    ports:
      - "8000:80"
    networks:
      - todo-app

networks:
  todo-app: